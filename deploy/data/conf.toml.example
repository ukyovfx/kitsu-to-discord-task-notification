# KitsuSync Configuration Example
#
# This is a SAMPLE file showing the deploy/data/ directory structure.
#
# For actual deployment configuration, use ../conf.toml.example from the
# repository root and refer to docs/SETUP_FOR_STUDIOS.md for detailed instructions.
#
# DO NOT commit real conf.toml files with live credentials to version control.
# Use environment variables (${VAR_NAME}) for runtime secrets instead.
#
# See: ../../../conf.toml.example
# See: ../../../docs/SETUP_FOR_STUDIOS.md
